'use client';import { useState, useEffect } from 'react';import { useParams } from 'next/navigation';import { InternalPage } from '@/components/InternalPage';import { ChevronLeft, ChevronRight } from 'lucide-react';import { parseQuestions, ParsedModule } from '../../parseQuestions';import { QuestionCard } from '../../QuestionCard';
export default function ModulePage(){const params=useParams();const slug=(params.moduleSlug as string)||'0';const mIdx=parseInt(slug.split('-')[0])||0;const[modules,setModules]=useState<ParsedModule[]>([]);const[qIdx,setQIdx]=useState(0);const[loading,setLoading]=useState(true);useEffect(()=>{import('../questionData').then(m=>{setModules(parseQuestions(m.default,'surgery','surgery-questions.md'));setLoading(false);}).catch(()=>setLoading(false));},[]);if(loading)return<InternalPage title="Loading..." subtitle="" introText="" backHref="/interns/jmc-exam-practice/surgery" backLabel="Surgery"><p className="text-center text-slate-400 py-8">Loading...</p></InternalPage>;const mod=modules[mIdx];if(!mod)return<InternalPage title="Not Found" subtitle="" introText="" backHref="/interns/jmc-exam-practice/surgery" backLabel="Surgery"><p className="text-slate-400">Module not found.</p></InternalPage>;const q=mod.questions[qIdx];const total=mod.questions.length;
return(<InternalPage title={mod.title} subtitle={`Question ${qIdx+1} of ${total}`} introText="" backHref="/interns/jmc-exam-practice/surgery" backLabel="Surgery"><div className="mb-6 flex flex-wrap gap-1.5">{mod.questions.map((_,i)=>(<button key={i} onClick={()=>setQIdx(i)} className={`flex h-8 w-8 items-center justify-center rounded-lg text-xs font-bold transition ${i===qIdx?'bg-cyan-500/30 text-cyan-300 border border-cyan-500/50':'bg-slate-800/50 text-slate-400 hover:bg-slate-700/50 hover:text-slate-300'}`}>{i+1}</button>))}</div>{q&&<QuestionCard q={q} total={total}/>}<div className="mt-6 flex items-center justify-between"><button onClick={()=>setQIdx(i=>Math.max(0,i-1))} disabled={qIdx===0} className="inline-flex items-center gap-1.5 rounded-xl border border-slate-700/40 bg-slate-900/40 px-4 py-2.5 text-xs font-bold text-slate-300 hover:border-cyan-500/30 disabled:opacity-30"><ChevronLeft size={14}/> Previous</button><span className="text-xs font-bold text-slate-500">{qIdx+1}/{total}</span><button onClick={()=>setQIdx(i=>Math.min(total-1,i+1))} disabled={qIdx===total-1} className="inline-flex items-center gap-1.5 rounded-xl border border-slate-700/40 bg-slate-900/40 px-4 py-2.5 text-xs font-bold text-slate-300 hover:border-cyan-500/30 disabled:opacity-30">Next <ChevronRight size={14}/></button></div></InternalPage>);}
